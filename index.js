import React, { Component } from 'react';
import { AppRegistry } from 'react-native';
import App from './src/App';

const WppClone = props => (
    <App/>
)

AppRegistry.registerComponent('AwesomeProject', () => WppClone);
